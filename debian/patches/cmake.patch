--- a/tool/CMakeLists.txt
+++ b/tool/CMakeLists.txt
@@ -11,6 +11,5 @@
 add_executable(corplot corplot.F90)
 target_link_libraries(fourier key2lower ${LAPACK_LIBRARIES})
 
-install(TARGETS fourier RUNTIME DESTINATION bin)
-install(TARGETS corplot RUNTIME DESTINATION bin)
-
+install(TARGETS fourier RUNTIME DESTINATION share/hphi/tool)
+install(TARGETS corplot RUNTIME DESTINATION share/hphi/tool)
--- a/src/komega/CMakeLists.txt
+++ b/src/komega/CMakeLists.txt
@@ -15,4 +15,3 @@
 if(MPI_FOUND)
   target_link_libraries(komega ${MPI_Fortran_LIBRARIES})
 endif(MPI_FOUND)
-install(TARGETS komega ARCHIVE DESTINATION lib LIBRARY DESTINATION lib RUNTIME DESTINATION bin)
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -10,7 +10,7 @@
 endif(NOT CMAKE_BUILD_TYPE)
 message(STATUS "Build type: " ${CMAKE_BUILD_TYPE})
 
-option(BUILD_SHARED_LIBS "Build shared libraries" ON)
+option(BUILD_SHARED_LIBS "Build shared libraries" OFF)
 option(ENABLE_MPI "Enable MPI Parallelization" ON)
 enable_language(C Fortran)
 
